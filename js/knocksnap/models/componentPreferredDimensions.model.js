define(['jquery'], function ($) {
    return function ComponentPreferredDimensions(minWidth, maxWidth, minHeight, maxHeight) {
        var self= this;
        self.minWidth = minWidth;
        self.maxWidth = minWidth;
        self.minHeight = minWidth;
        self.maxHeight = minWidth;

    }
});
